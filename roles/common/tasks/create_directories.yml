---
- name: Create user directories
  ansible.builtin.file:
    path: "{{ user_home }}/{{ item }}"
    state: directory
    mode: "0755"
  loop:
    - .config
    - .themes
    - .icons
    - Downloads
